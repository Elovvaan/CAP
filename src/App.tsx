function App() {
  return (
    <section className="empty-page">
      <div className="empty-icon">CAP</div>
      <h2>CAP Local Platform</h2>
      <p>
        The functional local CAP application is served from the built desktop
        launcher at <code>dist/index.html</code> and backed by SQLite through
        <code>launch-cap.cjs</code>.
      </p>
    </section>
  );
}

export default App;
